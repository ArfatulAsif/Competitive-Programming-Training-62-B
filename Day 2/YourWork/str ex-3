#include<iostream>
#include<string>
using namespace std;
int main()
{
    string s = "bikash";
    for(int i = 0; i < s.length(); i++)
    {
        if( i % 2 == 0)
        {
           s[i] = toupper(s[i]);
        }
    }
    cout << s << endl;
    return 0;

}